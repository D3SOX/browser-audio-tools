version: 2
updates:
  - package-ecosystem: "bun"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    # Always group React core libraries together in a single PR
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
